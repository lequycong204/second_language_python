def multiNum(a, b): #a, b là kiểu list
    c = []
    int_a = ""  # a viết dưới dạng số

    for i in a:
        int_a += str(i)

    for i in range(len(a)):
        rs = 0
    
    return c # c là 1 list